# Core engine + CLI need NOTHING beyond the Python standard library.
# The following are only needed for the optional web app and PDF parsing.
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
pypdf==4.2.0